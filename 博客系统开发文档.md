# 博客系统开发文档

> 版本：v1.0
> 最后更新：2026-09-08
> 文档状态：初稿
> 依赖项目：个人工作台（Personal Workbench，见《miaoda-code-个人工作台开发文档.md》）

---

## 目录

1. [项目概述](#1-项目概述)
2. [功能需求](#2-功能需求)
3. [技术方案](#3-技术方案)
4. [数据库设计（增量）](#4-数据库设计增量)
5. [API 接口设计（增量）](#5-api-接口设计增量)
6. [后端模块设计](#6-后端模块设计)
7. [前端模块设计](#7-前端模块设计)
8. [关键业务流程](#8-关键业务流程)
9. [开发计划与里程碑](#9-开发计划与里程碑)
10. [安全设计](#10-安全设计)
11. [附录](#11-附录)

---

## 1. 项目概述

### 1.1 定位

博客系统是「个人工作台」的**公开内容展示层**：用户在个人工作台创作并发布文章后，博客前台（Blog Frontend）面向所有访客公开展示这些文章，并提供分类、搜索、点赞、评论、转发等互动能力。

### 1.2 与现有系统的关系

博客系统**不是独立项目**，而是现有个人工作台的增量模块：

| 层 | 说明 |
|----|------|
| 数据 | 复用现有 `articles` / `categories`(type=2) / `tags` / `users` 表，增量新增评论、点赞记录、收藏、转发等表 |
| 后端 | 复用 FastAPI 应用，新增 `blog`、`comment` 等路由与服务，改造文章点赞逻辑 |
| 前端 | 新增博客前台（公开页面），复用现有个人工作台后台（创作/管理） |

### 1.3 核心目标

- 用户发布文章 → 博客前台即时可见（复用现有发布/下架机制）
- 访客可浏览、分类/标签导航、全文搜索、点赞、评论、转发分享
- 作者可查看数据（阅读/点赞/评论/转发统计）

---

## 2. 功能需求

### 2.1 已具备能力（复用，无需重复开发）

| 能力 | 现状 | 说明 |
|------|------|------|
| 文章创作 | ✅ | 后台草稿/发布/下架（§6.3.2 发布流程已实现） |
| 文章分类 | ✅ | `categories` 表 type=2（文章分类）已实现 |
| 文章标签 | ✅ | `tags` + `article_tags` 多对多已实现 |
| 全文搜索 | ✅ | `GET /articles/search`（MongoDB `$regex`）已实现 |
| 阅读量统计 | ✅ | `stat:article:view:{id}` Redis Hash 已实现 |
| 公开列表/详情 | ✅ | `GET /articles`、`GET /articles/{id}`（公开）已实现 |
| 用户认证 | ✅ | JWT 双 token 已实现（登录/点赞/评论等需登录的操作复用） |

### 2.2 新增功能清单

| 编号 | 功能 | 优先级 | 说明 |
|------|------|--------|------|
| F1 | **博客前台首页** | P0 | 文章流（分页）、最新/热门排序、分类/标签侧栏 |
| F2 | **文章详情页（公开）** | P0 | 正文渲染 + 点赞/收藏/分享 + 评论列表 |
| F3 | **评论系统** | P0 | 发表评论、楼中楼回复、删除自己的评论、评论点赞 |
| F4 | **点赞去重改造** | P0 | 现状为纯计数器可重复点赞，改造为按用户去重（toggle） |
| F5 | **文章收藏** | P1 | 用户收藏/取消收藏，个人收藏列表 |
| F6 | **转发/分享** | P1 | 复制文章链接分享（登录后记录统计），不做站内引用转发 |
| F7 | **分类/标签浏览页** | P0 | 按分类、按标签浏览文章 |
| F8 | **博客搜索页** | P0 | 复用全文搜索，独立搜索页 |
| F9 | **归档页** | P2 | 按年月归档 |
| F10 | **热门文章榜** | P1 | 按阅读量/点赞数排行（Redis 缓存） |
| F11 | **相关文章推荐** | P2 | 按同分类/同标签推荐 |
| F12 | **作者主页** | P2 | 展示某作者的全部已发布文章 |

### 2.3 访问与登录策略（已确认）

- 访客进入博客系统默认直达**博客主页**（`/blog`），**不跳登录页**。
- 访客可浏览文章、分类、标签、搜索、归档等全部只读内容（游客态）。
- **需登录的操作**：点赞、评论、收藏、转发记录。访客触发这些操作时，前端弹出**登录对话框**（不跳转页面），登录后自动继续原操作。
- 点赞**一人一次**（toggle 去重）；评论需登录（不支持匿名）。

---

## 3. 技术方案

沿用现有技术栈，**不引入新框架**：

- 后端：Python 3.10 + FastAPI + SQLAlchemy 2.0（异步）+ Motor + redis-py + Pydantic v2
- 前端：Vue 3 + TypeScript + Pinia + Element Plus + md-editor-v3 + Axios
- 存储：MySQL（评论/点赞/收藏/转发元数据）+ MongoDB（文章正文，复用）+ Redis（热门榜/阅读量/限流）
- 部署：复用现有 Docker Compose + Nginx（博客前台与后台共用同一前端应用，路由区分）

---

## 4. 数据库设计（增量）

> 复用表：`articles`、`categories`、`tags`、`article_tags`、`users`（均已有，不改结构）。
> 新增表均使用 `utf8mb4`、`InnoDB`。

### 4.1 评论表（comments）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 评论 ID |
| article_id | BIGINT | NOT NULL, INDEX | 所属文章 |
| user_id | BIGINT | NOT NULL, INDEX | 评论者 |
| parent_id | BIGINT | NULL, INDEX | 父评论 ID（NULL=顶级评论，非 NULL=楼中楼回复） |
| reply_to_user_id | BIGINT | NULL | 回复目标用户 ID |
| content | TEXT | NOT NULL | 评论内容（纯文本，前端渲染时转义防 XSS） |
| like_count | INT | DEFAULT 0 | 评论点赞数（冗余计数） |
| status | TINYINT | DEFAULT 1 | 0-删除 1-正常 2-待审核 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | ON UPDATE | 更新时间 |

```sql
CREATE TABLE IF NOT EXISTS comments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    article_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    parent_id BIGINT NULL,
    reply_to_user_id BIGINT NULL,
    content TEXT NOT NULL,
    like_count INT DEFAULT 0,
    status TINYINT DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_article (article_id),
    INDEX idx_user (user_id),
    INDEX idx_parent (parent_id),
    FOREIGN KEY (article_id) REFERENCES articles(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (parent_id) REFERENCES comments(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.2 文章点赞表（article_likes）—— 点赞去重

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK | 主键 |
| article_id | BIGINT | NOT NULL | 文章 ID |
| user_id | BIGINT | NOT NULL | 点赞用户 |
| created_at | DATETIME | DEFAULT | 时间 |

```sql
CREATE TABLE IF NOT EXISTS article_likes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    article_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_article_user (article_id, user_id),
    INDEX idx_article (article_id),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.3 文章收藏表（article_favorites）

```sql
CREATE TABLE IF NOT EXISTS article_favorites (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    article_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_article_user (article_id, user_id),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.4 转发记录表（article_shares）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK | 主键 |
| article_id | BIGINT NOT NULL | 文章 |
| user_id | BIGINT NULL | 转发用户（匿名分享为 NULL） |
| platform | VARCHAR(20) | 站内 weibo wechat link 等 |
| created_at | DATETIME | 时间 |

```sql
CREATE TABLE IF NOT EXISTS article_shares (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    article_id BIGINT NOT NULL,
    user_id BIGINT NULL,
    platform VARCHAR(20) DEFAULT 'link',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_article (article_id),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.5 评论点赞表（comment_likes）

```sql
CREATE TABLE IF NOT EXISTS comment_likes (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    comment_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_comment_user (comment_id, user_id),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 4.6 Redis 键新增

| Key 模式 | 类型 | TTL | 用途 |
|----------|------|-----|------|
| `cache:blog:feed:{page}:{size}:{sort}` | String(JSON) | 5 分钟 | 博客首页文章流缓存 |
| `cache:blog:hot` | ZSET 或 String(JSON) | 10 分钟 | 热门文章榜 |
| `rate:comment:{ip}:{path}` | String(计数器) | 1 分钟 | 评论接口限流 |

> 已有：`stat:article:view:{id}`（阅读量）、`cache:articles:*`（文章列表/详情缓存）。

---

## 5. API 接口设计（增量）

> 基础路径 `/api/v1`，统一响应 `{code, message, data}`，分页 `{items,total,page,page_size,total_pages}`。
> 认证：标记「是」的接口需 Bearer Token；「可选」表示登录后附带（用于返回「我是否已点赞/收藏」）。

### 5.1 评论接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/articles/{id}/comments` | 评论列表（分页，树形/扁平返回，按时间排序） | 否 |
| POST | `/articles/{id}/comments` | 发表评论（`{content, parent_id?, reply_to_user_id?}`） | 是 |
| DELETE | `/comments/{id}` | 删除评论（本人或文章作者） | 是 |
| POST | `/comments/{id}/like` | 评论点赞/取消（toggle） | 是 |

### 5.2 点赞（改造）

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/articles/{id}/like` | 点赞/取消点赞（按用户去重 toggle，返回 `{liked, like_count}`） | 是 |
| GET | `/articles/{id}/like` | 查询当前用户点赞状态（可选认证，未登录返回 `liked:false`） | 可选 |

> 改造点：现有 `like_count` 纯计数改为「article_likes 记录 + like_count 冗余计数」，保证一人一赞。

### 5.3 收藏接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/articles/{id}/favorite` | 收藏/取消收藏（toggle） | 是 |
| GET | `/favorites` | 我的收藏列表（分页） | 是 |

### 5.4 转发/分享接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/articles/{id}/share` | 记录一次分享（复制链接，`platform=link`），返回文章 URL | 是 |

### 5.5 博客前台聚合接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| GET | `/blog/feed` | 首页文章流（`sort=latest|hot`，分页） | 否 |
| GET | `/blog/hot` | 热门文章榜（Top N） | 否 |
| GET | `/blog/categories` | 分类列表（含文章数） | 否 |
| GET | `/blog/tags` | 标签云（含使用次数） | 否 |
| GET | `/blog/archive` | 按年月归档 | 否 |
| GET | `/blog/related/{article_id}` | 相关文章推荐（同分类/同标签） | 否 |

> 搜索复用现有 `GET /articles/search`；文章详情复用 `GET /articles/{id}`（改造后返回是否已点赞/已收藏/评论数/转发数）。

---

## 6. 后端模块设计

### 6.1 新增/改动文件

```
backend/app/
├── models/
│   ├── comment.py           # 新增：Comment、comment_likes
│   ├── favorite.py          # 新增：ArticleFavorite
│   ├── share.py             # 新增：ArticleShare
│   └── article_like.py      # 新增：ArticleLike
├── schemas/
│   ├── comment.py           # 新增：CommentCreate/CommentOut
│   └── blog.py              # 新增：FeedQuery/HotOut/CategoryStat/TagCloud
├── api/v1/
│   ├── comments.py          # 新增：评论路由
│   ├── blog.py              # 新增：博客前台聚合路由
│   └── articles.py          # 改造：点赞 toggle、详情返回点赞/收藏/评论数
├── services/
│   ├── comment_service.py   # 新增
│   ├── blog_service.py      # 新增：feed/hot/categories/tags/archive/related
│   └── article_service.py   # 改造：like 去重、detail 聚合互动数据
└── core/security.py         # 复用（认证依赖）
```

### 6.2 关键实现要点

1. **点赞去重**：`toggle_like` 先查 `article_likes` 唯一键，存在则删记录并 `like_count-1`，否则插记录并 `+1`；用唯一键兜底并发重复点击。
2. **评论**：顶级评论 `parent_id=NULL`；回复 `parent_id=目标评论 id`、`reply_to_user_id=被回复人`；返回树形结构（前端组装或后端递归组装）；软删除 `status=0`。
3. **评论计数**：文章详情返回 `comment_count`（`COUNT(comments WHERE article_id AND status=1)`），可缓存。
4. **热门榜**：定时或惰性按 `view_count + like_count` 加权排序，结果缓存 Redis 10 分钟。
5. **相关推荐**：取同分类 + 同标签交集优先，排除当前文章，限制 N 条。
6. **转发**：`article_shares` 记录用于统计，站内转发生成引用卡片（引用原文标题/摘要/链接）。

---

## 7. 前端模块设计

### 7.1 路由（博客前台，公开）

```
/blog                        # 首页文章流（F1）
/blog/articles/:id           # 文章详情（含评论/点赞/收藏/分享，F2）
/blog/categories             # 分类导航（F7）
/blog/categories/:id         # 某分类文章列表
/blog/tags                   # 标签云（F7）
/blog/tags/:tag              # 某标签文章列表
/blog/search                 # 搜索页（F8）
/blog/archive                # 归档页（F9）
/blog/users/:id              # 作者主页（F12）
```

> 现有公开路由 `/p/articles`、`/p/articles/:id` 由 `/blog/*` 体系取代，可保留重定向。

#### 7.1.1 博客前台路由说明（详见正文解释）

- `/blog` 是博客前台**公开入口**，无需登录即可访问；与个人工作台后台（`/`，需登录）同属一个前端应用，靠**路由前缀**区分。
- 博客前台使用独立布局 `BlogLayout`（博客顶栏 + 页脚）；后台使用 `Layout`（侧边栏 + 顶栏）。
- 访客在游客态触发点赞/评论/收藏/转发时，由全局「登录弹窗」组件（`LoginDialog.vue` + Pinia `useAuth`）拦截，登录后自动继续原操作，不跳转页面。
- 进入博客的默认入口即 `/blog` 首页，不做登录校验；后台入口 `/` 才做登录校验（无 token 跳 `/login`）。

### 7.2 页面/组件清单

| 类型 | 名称 | 说明 |
|------|------|------|
| 页面 | BlogLayout.vue | 博客前台布局（顶栏 + 底部，区别于后台 Layout） |
| 页面 | BlogHome.vue | 首页文章流 + 分类/标签侧栏 + 热门榜 |
| 页面 | BlogArticleDetail.vue | 详情：正文 + 点赞/收藏/分享按钮 + 评论 |
| 页面 | BlogCategory.vue / BlogTag.vue | 分类/标签浏览 |
| 页面 | BlogSearch.vue / BlogArchive.vue | 搜索 / 归档 |
| 组件 | ArticleCard.vue | 文章卡片（标题/摘要/封面/阅读/点赞） |
| 组件 | LikeButton.vue | 点赞按钮（含去重状态） |
| 组件 | FavoriteButton.vue | 收藏按钮 |
| 组件 | ShareButton.vue | 分享（复制链接/二维码） |
| 组件 | CommentList.vue | 评论列表（树形） |
| 组件 | CommentInput.vue | 评论/回复输入框 |
| 组件 | TagCloud.vue | 标签云 |

### 7.3 API 模块（前端）

```
src/api/blog.ts        # feed/hot/categories/tags/archive/related
src/api/comment.ts     # 评论 CRUD + 点赞
src/api/favorite.ts    # 收藏
src/api/share.ts       # 分享
src/api/article.ts     # 改造：like toggle、详情新增互动字段
```

---

## 8. 关键业务流程

### 8.1 发布文章 → 博客展示

```
后台「发布」→ article.status 0→1、published_at 写入
→ 清除 cache:blog:feed:* 与 cache:articles:* 缓存
→ 博客首页 /blog 下次请求重建 feed（仅 status=1）
```

### 8.2 访客浏览与互动

```
访客打开 /blog/articles/:id
→ 详情接口：正文(MongoDB) + 元数据(MySQL) + 阅读量 Redis 自增
→ 返回：content、view_count、like_count、comment_count、share_count、
        is_liked(登录时)、is_favorited(登录时)
→ 点赞：登录后 toggle → article_likes 写入/删除 + like_count 增减
→ 评论：登录后提交 → comments 写入 → 详情 comment_count 更新
```

### 8.3 评论树组装

```
GET /articles/{id}/comments
→ 取 status=1 的评论（按 created_at 升序）
→ 前端按 parent_id 组装树（顶级→子回复）
→ 每条返回：id、author(nickname/avatar)、content、like_count、
           reply_to 用户昵称、created_at
```

---

## 9. 开发计划与里程碑

| 阶段 | 周期 | 内容 | 交付物 |
|------|------|------|------|
| **B1 数据层 + 点赞改造** | 1-2 天 | 4 张新表迁移、article_likes 点赞去重、详情聚合互动字段 | 后端 + 测试 |
| **B2 评论系统** | 2-3 天 | 评论 CRUD + 树形返回 + 评论点赞 + 限流 | 后端 + 前端评论组件 |
| **B3 博客前台基础** | 3-4 天 | 首页 feed + 详情页 + 分类/标签/搜索页 | 前端页面 |
| **B4 收藏/分享/热门** | 2 天 | 收藏、分享、热门榜、相关推荐 | 前后端 |
| **B5 优化上线** | 2 天 | 归档/作者主页/安全加固/缓存优化/联调 | 完整博客系统 |

> 建议复用「个人工作台」已验证的开发方式：后端 mock 测试 + 真实库 E2E 联调 + worktree 并行隔离。

---

## 10. 安全设计

| 风险 | 措施 |
|------|------|
| 评论 XSS | 评论存纯文本，前端渲染时转义（不使用 v-html 渲染评论内容） |
| 评论/点赞刷量 | 评论接口 Redis 限流（如 5 条/分钟/IP）；点赞需登录且按用户去重 |
| 敏感内容 | 评论即时显示、作者可删；可选接敏感词过滤（`status=2 待审核`字段预留） |
| 越权 | 删除评论仅本人或文章作者；点赞/收藏校验登录态 |
| SQL 注入 | 全部走 SQLAlchemy ORM 参数化查询 |
| 转发滥用 | 站内转发需登录；分享链接只含公开文章 ID，不含敏感 token |
| 隐私 | 文章详情不返回作者 email 等敏感字段（仅 nickname/avatar） |

---

## 11. 附录

### 11.1 与现有系统的复用对照

| 现有模块 | 博客系统用途 |
|----------|-------------|
| articles + article_contents | 文章正文与元数据 |
| categories(type=2) | 文章分类 |
| tags + article_tags | 文章标签 |
| users | 作者/评论者 |
| auth（JWT） | 点赞/评论/收藏/转发认证 |
| Redis | 阅读量、feed/热门缓存、限流 |

### 11.2 迁移方式

新增 4 张表通过 Alembic 迁移（或补充 `deploy/init/init.sql`）；`article_likes` 上线时需处理历史 `like_count` 数据（可保留计数、去重从上线后开始生效）。

### 11.3 需求决策记录

| # | 事项 | 决策 | 状态 |
|---|------|------|------|
| 1 | 点赞去重 | 一个用户对一篇文章只能点赞一次（toggle 去重） | ✅ 已确认 |
| 2 | 登录态限制 | 点赞、评论、转发记录仅登录用户可操作 | ✅ 已确认 |
| 3 | 博客入口 | 进入博客默认直达主页，不跳登录页；需登录操作时弹窗询问 | ✅ 已确认 |
| 4 | 游客浏览 | 游客可浏览全部文章与只读内容 | ✅ 已确认 |
| 5 | 转发语义 | 转发 = 复制文章链接（外部分享），登录后记录统计 | ✅ 已确认 |
| 6 | 评论匿名 | 评论需登录，不支持匿名 | ✅ 已确认（由决策 2 推得） |
| 7 | 评论审核 | 即时显示 + 作者可删（`status=2 待审核`字段作为可选扩展预留） | ✅ 已确认 |
| 8 | 博客前台路由 | 采用 `/blog/*` 体系，详见 §7.1.1 | ✅ 待你确认 |
